import { StyleSheet } from 'react-native';

const useStyles = () =>
    StyleSheet.create({
        containerFAB: {
            flexDirection: 'row',
            width: '100%',
            justifyContent: 'flex-end'
        }
    });

export default useStyles;
