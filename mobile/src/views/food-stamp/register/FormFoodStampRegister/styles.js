//#region Imports

import { StyleSheet } from 'react-native';

//#endregion

const useStyles = () =>
    StyleSheet.create({
        container: {
            width: '100%'
        },
        content: {
            flexDirection: 'row'
        }
    });

export default useStyles;
