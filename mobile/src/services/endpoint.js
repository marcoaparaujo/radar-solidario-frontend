//#region Imports

import { LOCAL_BASE_URL } from '@env';

//#endregion

const ENDPOINT = {
    BASE: LOCAL_BASE_URL,
    SECURITY: {
        LOGIN: 'security/login'
    }
};

export default ENDPOINT;
